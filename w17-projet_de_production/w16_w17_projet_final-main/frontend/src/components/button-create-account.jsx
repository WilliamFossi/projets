import React from 'react'

const CreateAccountButton = ({ onClick }) => (
  <button type="button" className="create-account-button" onClick={onClick}>
    Créer un compte
  </button>
)

export default CreateAccountButton